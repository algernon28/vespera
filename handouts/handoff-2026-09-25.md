# Handoff: architecture simplification, state at 2026-09-25 18:35 UTC

**Purpose:** everything a new session needs to pick this work up cold. The session that wrote it kept hitting API rate limits. The plan itself is `handouts/architecture-simplification.md`.

---

## 1. Where everything is

### Pull requests (all drafts, none merged)

All three code PRs have main (`baa9a13`, ADR-151 from #298) merged in.

| PR | Branch @ head | What | Review | CI |
|---|---|---|---|---|
| **#292** | `claude/intelligent-faraday-eeslpa` | The plan and this file | sound with amendments; all applied | green |
| **#293** | `claude/289-unopenable-survivor` @ `8b0a65c` | #289 settled by ADR-152, plus the code change | `architect` passed | green; **merge-ready** |
| **#295** | `claude/wave-0a-test-base` @ `1d2aacb` | Wave 0a (#294), ADR-153: shared test slice, run-identity golden tests | `architect` passed | green; **merge-ready** |
| **#299** | `claude/wave-0b-upstream-choice` @ `22ecffd` | Wave 0b (#290, #296), ADR-154. Contains #293 and #295 | `architect` passed after one round | see the PR; local 731 run, 0 failures, 11 skipped; **merge-ready once green, after #293 and #295** |

**Merging is the owner's call; no agent merges.**

- **Merge order: #293, #295, then #299.** #299 contains both, so once they land its diff shrinks to Wave 0b alone.
- #293 and #295 have already been reconciled against each other inside #299. Whichever of them lands second conflicts on `AGENTS.md:35`, the ADR index, `Adr.java` and `RelevanceReportInvocationTest.java`'s imports; take #299's resolution of those files.
- **Operator cost:** #293 and #299 change `pipeline` (and #299 `ledger`), so merging them re-mints stages from 3 on. With #299 in, that no longer strands a working directory mid-campaign (that was #290), but it is still best done at a corpus boundary.

### Issues

| Issue | What | State |
|---|---|---|
| #289 | Relevance report fails on a file that won't open | settled by #293 (ADR-152) |
| #290 | A second run of one stage over a reused walk stops the next stage (a new build, or a profile value set mid-campaign) | decided by ADR-154; fixed in #299 |
| #291 | A seed file that can't be opened fails seed extraction instead of becoming an unusable seed | open, not started |
| #294 | Wave 0a | done in #295 |
| #296 | An old arrangement approval keeps opening 6b's gate over a replaced arrangement | decided by ADR-154; fixed in #299 |
| #297 | Loosening a floor brings nothing back over a reused walk: blocking verdicts from any run still count | open, not decided (measured by a throwaway probe) |

Also open, not touched here: #286 and #287. ADR-150 and ADR-151 are reserved for them.

---

## 2. Wave 0b in detail (the live work)

### Decided: ADR-154

The file is `docs/adr/0154-…md`, on the Wave 0b branch.

- **#290:** a stage names as its upstream run the run of the stage before it that *this invocation* minted or continued. It no longer looks runs up over the walk. Since ADR-115 every invocation starts at census, so the id is always in hand.
  - To go back to an earlier run, the operator puts the value back.
  - `AmbiguousUpstreamRunException` is deleted. `NoUpstreamRunException` stays, reworded as a wiring defect.
- **#296:** the approval opens 6b only if it names the arrangement this invocation minted or continued. `AmbiguousArrangementException` is deleted, and `NextAction` gets a new sentence for a stale approval.
- **Also settled by ADR-154:**
  - 6b must not reach `ArrangementRun` itself; it would mint behind the gate.
  - `arrangement.html` is re-rendered from recorded rows when a continued arrangement's step has already finished.
- **Amends** ADR-099, ADR-107, ADR-115 (that one page only) and ADR-058 (its Consequences: per-build cost).
- **Rejected:** ADR-099's own gate, build-only narrowing, recency, deleting superseded runs.

### Tests, complete (analyst)

- `UpstreamRunOverAReusedWalkTest` (#290) and `ArrangementApprovalOverAReusedWalkTest` (#296), plus `ledger/SuccessiveBuildsBeans`.
- **10 new tests.** All were red on the pre-change code for the right reason: the ambiguity refusal, or the stale approval. The other 718 were green.

### Implementation, DONE (spec-implementer) at `441da9f`

- `InvocationRuns` wraps the job execution's `ExecutionContext`. It is not a bean.
- Each run-minting site records the run it arrived at, and `UpstreamRuns.runOf(stage)` reads it back.
- `ArrangementGate.approvedArrangement(Optional<RunId>)` matches only this invocation's arrangement.
- 6b never reaches `ArrangementRun` itself, and `arrangement.html` is re-rendered from the recorded rows.
- `Ledger` loses `runsOf`, `runsMatching` and `latestRunFor`. The two ambiguity exceptions and `RecordedRun` are deleted.
- **Results:** the 10 new tests pass, and `RunIdentityGoldenTest` stays green. The test tree does not compile until step 2 below.
- **Four tests beyond item 7 break.** Three fixtures in `GenerationInvocationTest`/`GenerationBreakerInvocationTest` insert, by raw SQL, a cluster with no members, which the re-render now rejects (ADR-112). The fourth, `GenerationInvocationTest.stopsWhenTheApprovalMatchesTwo`, pins a refusal ADR-154 retires.
- **Decision:** fix the fixtures (reach ADR-121's "nothing can be sent" from a state an arrangement really records), not narrow the re-render.

### Remaining steps, in order

1. ~~`spec-implementer`: finish ADR-154 items 1–8.~~ Done at `441da9f`.
2. **`analyst`:** item 7's tests are re-expressed and `stopsWhenTheApprovalMatchesTwo` is retired (`92b9b19`; suite 724 run, 3 failures).
   - **The 3 failures are the raw-row fixtures.** No realistic unsendable cluster was reachable while the relevance report re-chunked every sampled document.
   - **#293 (ADR-152) makes that report read the cache only, so it was merged into the branch** (`9de5922`, conflicts resolved: 152 decisions, rows 152–154).
   - **In progress when this was written:** re-express those three fixtures by rewriting one document in place (same length, modified time restored), so its new bytes are never chunked. If possible, also make `leavesTheStepOpenWhenAClusterCouldNotBeWrittenOver` non-vacuous; today it passes only because its deleted files give a new walk and a shut gate.
   - **Uncommitted in the worktree when this was written:** a javadoc fix in `ExtractionRun`, `ContentCensusRun`, `RedundancyRun` and `SeedMeasurementRun` ("look the run up" becomes "read the run this invocation recorded"). Redo it if lost. (`UpstreamRunsTest`, `ArrangementGateTest`, fixtures in `ExtractionRunTest`/`ContentCensusRunTest`/`ContentCensusTaskletTest`/`ExtractionItemProcessorTest`, `LedgerTest`'s `latestRunFor` tests), now that the new types exist.
3. **Full `./mvnw -B test`:** everything green, including `RunIdentityGoldenTest` (run identities must not move) and the 10 new tests.
3a. ~~Fixtures and full suite~~: done at `8a9d1a6` (727 run, 0 failures, 11 skipped).
4. ~~`architect` gate on #299~~: sound with amendments (I1–I3 for `spec-implementer`, B2 and A1–A5 for `analyst`). All applied in `56ae213`, including A4: `vespera label` with the floor set now says what the next `vespera run` will do (ADR-154 §2, two new `ClosingLineInvocationTest` tests). Suite 731 run, 0 failures, 11 skipped; claims and render pass.
5. ~~Re-gate~~: passed on `56ae213`, no blocking amendments. Its one optional wording note on ADR-154 is applied in `22ecffd`. **Nothing remains for Wave 0b but the owner's merges.**

---

## 3. Environment (cloud container)

- **JDK 26 is required and is not in the image**, which ships Java 21. The environment's network policy allows Oracle's download host, and denies `api.adoptium.net`, `download.java.net`, `cdn.azul.com`, `corretto.aws` and `api.sdkman.io`.
- **Put this in the environment's Setup script** (cloud environment menu in the session title bar, then Edit). It downloads and checksum-verifies Oracle JDK 26, then makes it the default in login shells along with a UTF-8 locale:

  ```bash
  #!/bin/bash
  set -euo pipefail
  JDK=/opt/jdk-26
  if [ ! -x "$JDK/bin/java" ]; then
    url=https://download.oracle.com/java/26/latest/jdk-26_linux-x64_bin.tar.gz
    curl -fsSL -o /tmp/jdk-26.tar.gz "$url"
    echo "$(curl -fsSL "$url.sha256")  /tmp/jdk-26.tar.gz" | sha256sum -c -
    mkdir -p "$JDK" && tar -xzf /tmp/jdk-26.tar.gz -C "$JDK" --strip-components=1 && rm /tmp/jdk-26.tar.gz
  fi
  cat > /etc/profile.d/jdk26.sh <<'EOF'
  export JAVA_HOME=/opt/jdk-26
  export PATH="$JAVA_HOME/bin:$PATH"
  export LANG=C.UTF-8 LC_ALL=C.UTF-8
  EOF
  ```
- **Run Maven through a login shell:** `bash -lc 'cd <checkout> && ./mvnw -B test'`. Without `-l` you get Java 21 and the POSIX locale, and two `OccurrencePathTest` tests error on non-ASCII names.
- **Tests run as root here**, so the permission-based tests skip; 9 skips is the baseline, and 11 on #293's branch. Use `setpriv --inh-caps=-dac_override,-dac_read_search` to exercise them.
- **Worktrees** under `.claude/worktrees/` are gitignored and die with the container. Everything that matters is pushed.

---

## 4. Open follow-ups (none blocks the PRs)

- **Test-support-package premise:** ADR-052 and ADR-119 say a test-only package would read as a ninth module to `ApplicationModules`. Spring Modulith 2.1.0's bytecode suggests `DoNotIncludeTests`. Measure it with a throwaway test and record the result.
- **Wave 0a nits:** `RunIdentityGoldenTest.java:36` is a 134-character javadoc line. `CascadeSliceImportsTest.java:73`'s claim says "the first and the last written" while naming three classes.
- **Left open by ADR-154:** #297; other report pages going stale when a value is put back; minimum approval-prefix length; invocations starting part-way down the cascade.
- **Stale agent definitions:** see the plan's D7. `VesperaApplicationTests` doesn't exist, and the test-yaml note is wrong.
- **Later waves:** 1 through 6 (and deferred 7) per the plan. Charting a wayfinder map for them means editing `AGENTS.md:44-46` in the same change, or CI's claims job fails.

## 5. Resuming prompt

> Read `handouts/handoff-2026-09-25.md` on branch `claude/intelligent-faraday-eeslpa`. Check out `claude/wave-0b-upstream-choice` in a worktree. Continue from §2 "Remaining steps", at the first step not yet done, then Wave 1 per the plan once the owner has merged #293, #295 and #299. Follow AGENTS.md's agent roles, and keep the PRs green and unmerged.
